# Task Tracker CLI — Runtime Dependencies
# Python 3.10+ required (matches vLLM workspace pyproject.toml)

# CLI framework — declarative, composable, with built-in CliRunner for testing
click>=8.1,<9.0

# Terminal UI — first-class tables, panels, and colors
rich>=13.0,<14.0

# ──────────────────────────────────────────────
# Development / Testing dependencies
# ──────────────────────────────────────────────
pytest>=7.0,<9.0
